I export my character out of the game into a planner build, appearance included,
so one shared link carries the face as well as the gear.

Storing it works: the build comes back from the API with a full `sliders` object.
But when I load that build, the Cosmetics tab says "No cosmetics data" and there
is no Copy AOB button, so the appearance is gone by the time anyone opens the
link.

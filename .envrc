# shellcheck shell=bash
# Load local environment fragments for er-effects-rs.
# Env fragments live in ./.envs/*.env and are ignored by git.

env_dir="$PWD/.envs"

if [ -d "$env_dir" ]; then
  for env_file in "$env_dir"/*.env; do
    [ -e "$env_file" ] || continue
    set -a
    # shellcheck source=/dev/null
    source "$env_file"
    set +a
  done
fi

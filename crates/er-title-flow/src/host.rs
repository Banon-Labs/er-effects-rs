// Host boundary: function pointers the root DLL installs at bootstrap
// (er-loading-portrait `install_host` precedent). Populated during Stage B
// of the extraction as the compiler enumerates the true callback surface.

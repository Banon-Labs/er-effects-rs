readiness_strategy=Runtime probes are disabled fail-closed. The aborted 2026-06-13 probe showed that launcher/process following plus DLL terminal telemetry is insufficient when telemetry never starts.
structured_failure=Required before re-enable: a deterministic no-telemetry bootstrap failure path, in addition to process exit, save-hash mismatch, teardown evidence, and DLL-authored game-task tick budgets.
user_impact=Runtime is not launched by autoresearch or direct runtime_probe.sh, even with explicit opt-in, until the no-telemetry gap is fixed and the policy/checker/tests are updated together.

# Autoresearch Ideas

- LIVE EVIDENCE (autoload-live-20260614, zero simulated input): cold launch boots fast (ASLR-disabled + skip-splash) straight to the "PRESS ANY BUTTON" title with `game_man_available=true` but `title_bootstrap_seen=false` and `state=0`. With `require_title_bootstrap=false`, the DLL fires the direct continue sequence immediately: continue-trace shows `flags{b72=1,b73=1}` set at `state=0,b80=0,bbc=1,bc0=0,bc4=0,req_idx=-1,b78=-1`, but the flags are NEVER consumed — `player_available` stays false and `state` stays 0 while the menu_task_enqueue hook keeps enqueuing only title-screen nodes (callers b0d221/b0c443/b0be57). Conclusion: the MoveMapList save-load dispatcher that reads b72/b73 only runs AFTER title accept advances the menu past state=0; setting load flags pre-bootstrap is a no-op. The missing native transition is exactly the title-accept/menu-advance the first safe-input Confirm substitutes for. Next: gate the direct continue sequence on `title_bootstrap_seen` (set `require_title_bootstrap=true`) AND/OR drive the native title-accept that moves state 0 -> menu so b72/b73 get consumed; do not fire load flags at state=0.
- BIG PROGRESS (live autoload-live-playgame-v8-20260614): native zero-input PlayGame drive now sets the load value and enqueues the load job. force_play_game writes: owner+0xbc = slot 9 (the field STEP_PlayGame's handler reads via `mov eax,[rdi+0xbc]`, NOT the +0xac0 save slot), owner+0x3e1 = 1, owner state = 5. RESULT: PlayGame ran, the submit->validate->pair chain wrote GameMan+0x14 = 9 (continue-trace load14 went -1 -> 9 at the 5->6 transition), the title state ADVANCED to 6 STEP_GameStepWait and HELD (no longer resets 6->2), and menu_task_enqueue callers switched to the load-submission path (0xaf2e4c/0xaec1f7/0xaeba07 next to play_game_submit 0xaebdc0). REMAINING GAP: the enqueued load job does not execute to completion — GameStepWait holds ~40s, no map_load_67bc10 fires (title_bootstrap_seen stays false), GameMan save_state stays 0, player_available stays false. Decoded chain (readonly): PlayGame 0x140b0d5b0 reads owner+0xbc -> submit 0x140aebdc0 -> validate 0x67aec0 (gates 12d/12e) -> pair 0x67abd0 (requires GameMan+0xb28==0 [confirmed 0] and slot!=-1, writes GameMan+0x14). Precondition snapshot: ac0=9 b28=0 12d/12e vary (not blocking) load14:-1->9. NEXT: find why the enqueued GameStepWait load job never triggers the actual map load — likely needs the menu/save-load task to be pumped/processed, or a "begin load" trigger consuming GameMan+0x14. Investigate GameStepWait handler 0x140b0cde0 and what consumes GameMan+0x14.
- PROGRESS (live autoload-live-playgame-v3-20260614): the TitleStep owner is now FOUND and FPS stays healthy (~60/s) after fixing two bugs: (1) TITLE_OWNER_VTABLE_RVA was 0x2b63ba0 but the ctor at 0x140b0b1c0 stores 0x2b63bb0 to owner+0 (off by 0x10) — corrected; (2) the owner scan ran every frame — throttled. Live the TitleStep owner idles at state 10 = STEP_MenuJobWait (not BeginTitle). force_play_game now fires: `set slot=9 state 10->5 (STEP_PlayGame)` and set_save_slot(9) takes effect (continue-trace slot flips -1 -> 9). BUT the bare state->5 jump does NOT complete a load: the title task just rebuilds its menu nodes (callers b0c460/b0c68a/b0c6f3/b0c71e/b0c760/b0be57), GameMan save_state stays 0, no b72/b73, player_available stays false. CONCLUSION: STEP_PlayGame (0x140b0d5b0) needs preconditions beyond slot+state — it reads a selected menu value and submits an owner job (play_game_submit 0x140aebdc0: validates selected value then load pair, returns early when slot==-1, stores load pair to owner_job+0x100/0x104, appends payload to owner_job b35f0). NEXT: either (a) call play_game_submit/handler with the proper owner-job + selected-value setup, or (b) drive the menu "Continue"/PlayGame selection that populates owner_job before the state->5 jump. The owner-resolution + FPS path is solved; remaining gap is PlayGame's job/selected-value precondition.
- FPS-cripple bug found + fixed (live autoload-live-playgame-20260614): the new `force_play_game` path (set slot 9, write TitleStep owner state +0x4c = 5 STEP_PlayGame, let the native title task dispatch the load) never fired because `title_owner()`/`find_title_owner_by_vtable` did a FULL address-space VirtualQuery walk EVERY frame while the owner was unmatched, collapsing the game's frame task to ~1.7-2.0 Hz (healthy ~60). The TitleStep owner vtable scan (TITLE_OWNER_VTABLE_RVA=0x02b63ba0) found no match → no debug output, no state write. Fix landed: throttle the scan to ~once/sec (TITLE_OWNER_SCAN_CALL_INTERVAL). New metric `runtime_frame_task_hz_min/_avg` added (prompt + .auto/frame_task_hz.py) with a perturbation hard-gate. NEXT: the owner-by-vtable scan does not match the live TitleStep owner — stop brute-scanning; resolve the owner from the title global context (~0x143d6b7b0) or the state-table global the dispatcher uses, then retry force_play_game (state +0x4c -> 5) which is still the most promising zero-input native load route. Also fold the working boot-then-poll + save-safety + frame_task_hz workflow into the harness (the Rego/watcher 30s cap cannot cold-launch).
- Do not retry `direct_bootstrap_pump` unchanged (live autoload-live-pump-20260614, zero simulated input): it OSCILLATES state 0<->1 and never loads. Each cycle: `combined_load(-1,0,1)` sets save_state 0->1, then `save_load_pump_default 0x679510` holds state=1 for ~2 ticks, state resets to 0, repeat — 1777 attempts in 30s, `player_available` never true. Conclusions: (1) `0x679510` is NOT the menu-task pump that advances/consumes the load — it just holds/resets save_state=1; the real pump is the post-title MoveMapList menu task. (2) The `0x67a310` "title bootstrap marker" does NOT flip `title_bootstrap_seen` (which is gated on the `map_load_67bc10` hook returning success) — so `0x67a310` is not the real title-accept. Both reinforce that the load primitives cannot complete unless the game is in the actual post-title main-menu state where the native MoveMapList task runs. Next: find/drive the native title-accept -> main-menu transition (the one map_load_67bc10 participates in), NOT more direct load-primitive/pump permutations.
- Add a tiny Ghidra/export script that emits machine-readable refs around GameMan `+0xb72/+0xb73/+0xb78/+0xbc4` and the `0x140af*` MoveMapList/title-menu functions so `measure.sh` can score exact static RE evidence without launching ER.
- Consider moving the kept fallback-backed DirectInput/key-state safe-input backend behind a cleaner `er-safe-input` abstraction so production code exposes logical Confirm pulses without host pointer/focus nudges or ad-hoc hook code in `src/lib.rs`. DirectInput-only was flaky; preserve the fallback behavior unless a state-gated replacement proves stable.
- Static-RE the menu task wrappers around `0x14082bb00` and `0x14082a0f0`: standalone `map_load_67bc10` strands `save_state=1`, so the missing transition is likely the task wrapper/pump that advances or resets `b80/bb8/bbc/bc0/bc4` after map/load requests.
- If runtime probing is explicitly authorized, reintroduce telemetry-only fields for branch gate `0x143d6f9c0` plus GameMan `+0xb5e/+0xb5f/+0xb60/+0xac0/+0xbcc/+0xbcd/+0xbce` and run one bounded no-pointer probe to confirm whether current autoload falls into e780 reset-to-zero or e650 restore-selected path.
- Prototype a non-completing DirectMenuLoad state machine that keeps polling after the first queue when TitleStep::Finish resets `set_save_slot(-1)` at `0x140b0cd8b`; current code marks completed immediately after queuing, which may prevent a deterministic requeue in the next safe title/menu state.
- DirectInput-gated fixed-count path has minimum known-good count 6 at 110-tick spacing for the currently observed menu stack, but required Confirms are nondeterministic because pre-Continue popups vary (e.g. unsafe shutdown warning). Next optimize by state-gating/menu-awareness or native Continue/menu transition; use high maximum budgets only if actual emitted pulses are measured and the driver stops on structured player-load evidence.
- Strengthen the 1000 success oracle beyond `PlayerIns` availability if practical: capture a post-load artifact or effect-activation proof while preserving save restore, no host input, and clean `driver_rc=0`.
- Consider a scripted debugger-style attach probe (breakpoint-at-first-relevant-moment plus targeted later breakpoints) as a deterministic evidence collector for title/menu/load transitions; keep it non-manual, bounded, and integrated with runtime teardown/save-restore gates.
- If visual input-reason evidence is needed again, implement a process-group-safe screenshot sidecar or in-driver capture path. The first sidecar attempt captured useful `input-before-*.jpg` evidence (title `PRESS ANY BUTTON`, Seamless welcome/OK, save-data updates) under `/home/banon/projects/er-effects-rs/target/smoke/autoload-runtime-20260614-002640`, but it left the wrapper alive until the 900s experiment timeout.
- Treat safe-input/OCR paths as probe-only scaffolding: the next primary research direction is mapping the native zero-input startup/menu/load sequence that replaces title accept, Seamless dialogs, save-data prompts, Continue, and post-map final Confirm entirely inside the DLL before loading the selected character.
- Do not retry the standalone native global +0x18 title-toggle experiment unchanged: zero-input runtime `autoload-runtime-20260614-172820` only waited/queued and did not reach title bootstrap. If revisiting this path, first map or trace the paired +0x19/extra-parent callback chain that b0d400/BeginTitle uses around global context 0x143d6b7b0.
- Do not spend more live runtime attempts on the native extra-parent callback `0x140b01be0` until the owner/state trace point is passive and launch mode is stable. Steam no-spawned at `autoload-runtime-20260614-174019`; direct-protected exited before ready at `autoload-runtime-20260614-174356`.
- Do not retry the reconstructed b0d4ad native title-accept task submission unchanged: runtime run `autoload-runtime-20260614-100114` visibly slowed/perturbed the splash screen and exhausted readiness before useful evidence. Prefer static mapping of b0d4ad/9b26d8 task payload ownership or a passive task-node tracer before any further live submission.
- Do not retry the `0x143d856a0` native title accept gate mutation: runtime run `autoload-runtime-20260614-103436` reduced framerate enough that the game showed a refusal/extra menu, and no request consumption was observed. Treat live title-accept branch mutation as user-visible perturbation; pivot to offline/static mapping or strictly passive/narrow traces only.

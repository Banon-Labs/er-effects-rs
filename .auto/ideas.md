# Autoresearch Ideas

- Add a tiny Ghidra/export script that emits machine-readable refs around GameMan `+0xb72/+0xb73/+0xb78/+0xbc4` and the `0x140af*` MoveMapList/title-menu functions so `measure.sh` can score exact static RE evidence without launching ER.
- Consider moving the kept fallback-backed DirectInput/key-state safe-input backend behind a cleaner `er-safe-input` abstraction so production code exposes logical Confirm pulses without host pointer/focus nudges or ad-hoc hook code in `src/lib.rs`. DirectInput-only was flaky; preserve the fallback behavior unless a state-gated replacement proves stable.
- Static-RE the menu task wrappers around `0x14082bb00` and `0x14082a0f0`: standalone `map_load_67bc10` strands `save_state=1`, so the missing transition is likely the task wrapper/pump that advances or resets `b80/bb8/bbc/bc0/bc4` after map/load requests.
- If runtime probing is explicitly authorized, reintroduce telemetry-only fields for branch gate `0x143d6f9c0` plus GameMan `+0xb5e/+0xb5f/+0xb60/+0xac0/+0xbcc/+0xbcd/+0xbce` and run one bounded no-pointer probe to confirm whether current autoload falls into e780 reset-to-zero or e650 restore-selected path.
- Prototype a non-completing DirectMenuLoad state machine that keeps polling after the first queue when TitleStep::Finish resets `set_save_slot(-1)` at `0x140b0cd8b`; current code marks completed immediately after queuing, which may prevent a deterministic requeue in the next safe title/menu state.
- DirectInput-gated fixed-count path has minimum known-good count 6 at 110-tick spacing for the currently observed menu stack, but required Confirms are nondeterministic because pre-Continue popups vary (e.g. unsafe shutdown warning). Next optimize by state-gating/menu-awareness or native Continue/menu transition; use high maximum budgets only if actual emitted pulses are measured and the driver stops on structured player-load evidence.
- Strengthen the 1000 success oracle beyond `PlayerIns` availability if practical: capture a post-load artifact or effect-activation proof while preserving save restore, no host input, and clean `driver_rc=0`.
- Consider a scripted debugger-style attach probe (breakpoint-at-first-relevant-moment plus targeted later breakpoints) as a deterministic evidence collector for title/menu/load transitions; keep it non-manual, bounded, and integrated with runtime teardown/save-restore gates.
- If visual input-reason evidence is needed again, implement a process-group-safe screenshot sidecar or in-driver capture path. The first sidecar attempt captured useful `input-before-*.jpg` evidence (title `PRESS ANY BUTTON`, Seamless welcome/OK, save-data updates) under `/home/banon/projects/er-effects-rs/target/smoke/autoload-runtime-20260614-002640`, but it left the wrapper alive until the 900s experiment timeout.
- Treat safe-input/OCR paths as probe-only scaffolding: the next primary research direction is mapping the native zero-input startup/menu/load sequence that replaces title accept, Seamless dialogs, save-data prompts, Continue, and post-map final Confirm entirely inside the DLL before loading the selected character.
- Do not retry the standalone native global +0x18 title-toggle experiment unchanged: zero-input runtime `autoload-runtime-20260614-172820` only waited/queued and did not reach title bootstrap. If revisiting this path, first map or trace the paired +0x19/extra-parent callback chain that b0d400/BeginTitle uses around global context 0x143d6b7b0.
- Do not spend more live runtime attempts on the native extra-parent callback `0x140b01be0` until the owner/state trace point is passive and launch mode is stable. Steam no-spawned at `autoload-runtime-20260614-174019`; direct-protected exited before ready at `autoload-runtime-20260614-174356`.
- Do not retry the reconstructed b0d4ad native title-accept task submission unchanged: runtime run `autoload-runtime-20260614-100114` visibly slowed/perturbed the splash screen and exhausted readiness before useful evidence. Prefer static mapping of b0d4ad/9b26d8 task payload ownership or a passive task-node tracer before any further live submission.
- Do not retry the `0x143d856a0` native title accept gate mutation: runtime run `autoload-runtime-20260614-103436` reduced framerate enough that the game showed a refusal/extra menu, and no request consumption was observed. Treat live title-accept branch mutation as user-visible perturbation; pivot to offline/static mapping or strictly passive/narrow traces only.
